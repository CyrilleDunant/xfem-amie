.damage_model = PrandtlGrauertPlasticStrain
..compressibility = 0.05 # (value) @value[limit_strain] 0.0057 //
..limit_strain = 0.0057 # (value) @value[initial_plastic_variable] 0 //
..initial_plastic_variable = 0 # (value) */
.damage_model = SpaceTimeFiberBasedPlasticStrain
..damage_increment = 1e-6 # (value) damage increment which is applied at each step of the damage algorithm
..time_tolerance = 1e-5 # (value) time lapsed between two damage events
..maximum_damage = 0.999 # (value) damage above which an element is considered broken
..fixed_orientation = false # (string) fix the plastic direction once it has been set
.damage_model = SpaceTimeFiberBasedPlasticDamage
..damage_function = # (string) gets the damage variable as a function of the plastic strain
..requirements = # (string) defines the extra material variables needed to calculate the damage
..damage_increment = 1e-6 # (value) damage increment which is applied at each step of the damage algorithm
..time_tolerance = 1e-5 # (value) time lapsed between two damage events
..maximum_damage = 0.999 # (value) damage above which an element is considered broken
..fixed_orientation = false # (string) fix the plastic direction once it has been set
.damage_model = FiberBasedIsotropic
..damage_increment = 0.1 # (value) damage increment which is applied at each step of the damage algorithm
..maximum_damage = 0.6 # (value) damage above which an element is considered broken
.damage_model = SpaceTimeFixedPointIsotropic
..damage_increment = 0.1 # (value) damage increment which is applied at each step of the damage algorithm
..maximum_damage = 0.6 # (value) damage above which an element is considered broken
.damage_model = SpaceTimeSequentialIsotropic
..damage_increment = 0.1 # (value) damage increment which is applied at each step of the damage algorithm
..time_tolerance = 1e-9 # (value) minimum time between two successive damage events
..maximum_damage = 0.6 # (value) damage above which an element is considered broken
.damage_model = SpaceTimeFiberBasedBilateral
..damage_increment = 0.1 # (value) damage increment which is applied at each step of the damage algorithm
..time_tolerance = 0.001 # (value) minimum time between two successive damage events
..maximum_damage = 0.6 # (value) damage above which an element is considered broken
..secondary_maximum_damage = -1 # (value) damage above which an element is considered broken
..@string<IsotropicMaterialParameters> = BULK_SHEAR # (string) independent material parameters affected
..@string<planeType> = PLANE_STRESS # (string) 2D planar assumption
.damage_model = SpaceTimeFiberBasedFixedCrack
..damage_increment = 0.1 # (value) damage increment which is applied at each step of the damage algorithm
..time_tolerance = 1e-5 # (value) time lapsed between two damage events
..maximum_damage = 0.999 # (value) damage above which an element is considered broken
..orientation_field = REAL_STRESS_FIELD # (string) field used to get the orientation of the crack
..external_orientation = false # (string) find the orientation from the ElementState
..cleavage = false # (string) sets one crack instead of two
.damage_model = SpaceTimeFiberBasedIsotropic
..damage_increment = 0.1 # (value) damage increment which is applied at each step of the damage algorithm
..time_tolerance = 0.001 # (value) minimum time between two successive damage events
..maximum_damage = 0.6 # (value) damage above which an element is considered broken
.damage_model = Isotropic
.damage_model = PrandtlReussPlasticStrain
